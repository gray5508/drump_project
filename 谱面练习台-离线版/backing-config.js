(function () {
  "use strict";

  // 统一配置文件：由页面中的“导出统一配置”生成并替换此文件。
  // 提交到 GitHub 后，未做本机修改的电脑、手机和平板会读取同一套参数。
  window.DRUM_BACKING_CONFIG = {
    version: 1,
    revision: "",
    exportedAt: "",
    globalRate: 1,
    segments: {}
  };
})();
